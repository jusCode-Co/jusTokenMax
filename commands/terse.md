---
description: Switch to terse output mode to reduce the tokens the agent writes.
argument-hint: "[lite|full|ultra]"
---

Adopt the `terse-output` style for the rest of this session at level
**$ARGUMENTS** (default `full`): lead with the answer, fragments over
sentences, bullets over paragraphs, no preamble or filler — while keeping all
code, commands, paths, identifiers, numbers, and caveats exact.

Acknowledge in one line, then apply it from the next response onward.
